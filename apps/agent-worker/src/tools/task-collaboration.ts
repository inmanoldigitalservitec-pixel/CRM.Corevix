import type { ToolContext, ToolResult } from "../types";

export async function getTaskDetailTool(ctx: ToolContext, args: any): Promise<ToolResult> {
  return { ok: false, error: "Task collaboration tools placeholder." };
}
