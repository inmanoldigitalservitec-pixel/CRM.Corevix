<IfModule mod_rewrite.c>
  RewriteEngine On

  # SPA fallback (TanStack Router): serve index.html for client-side routes
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule ^ index.html [L]
</IfModule>

