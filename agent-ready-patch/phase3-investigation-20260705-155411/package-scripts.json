{
  "dev": "vite dev",
  "build": "NODE_OPTIONS=--max-old-space-size=4096 WRANGLER_LOG_PATH=/private/tmp/wrangler-logs vite build",
  "build:dev": "vite build --mode development",
  "preview": "vite preview",
  "lint": "eslint .",
  "format": "prettier --write ."
}
